/*interface ItemProps {
    name: string;
    isPacked: boolean;
  }
  
  export default function Item({ name, isPacked }: ItemProps): JSX.Element {
    //Long version
    /*if (isPacked){
        return <li>{name} ✔</li>;

    }
    return <li>{name}</li>;
    
    return(
      <li>{isPacked ? name + '✔' : name + '❌'}</li>
    )
  }*/
  